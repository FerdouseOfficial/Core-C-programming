#include <stdio.h>
#include <math.h>

int main() {

    float x = 3;

    // x = sqrt(x);
    // x = pow(x, 2);
    // x = round(x); If the point value is above 500 it will make it 10 I mean add one  there  ;
    // x = ceil(x); No matter what the value it will make it 10 I mean add 1 to the value and make it a round figure;
    // x = floor(x); No matter what the value it will make all the point number useless it will only show the actual integer number only 
    // x = abs(x); 
    // x = log(x);
    // x = sin(x);
    // x = cos(x);
    // x = tan(x);


    printf("%f", x);

    return 0;
}